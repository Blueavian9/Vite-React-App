import { Button } from '@supabase/ui'

export default Button = () => {
    return ( <div>
                <Button>I am a Supabase UI button</Button>  
                </div>
            );
        };
